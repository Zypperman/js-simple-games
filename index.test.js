const main = require('./index.js');
import 'jest-dom/extend-expect';

// test whether user can enter a username
/*
test('username works', () => {
    expect;
});

describe('My function', () => {
    let consoleSpy;

    beforeEach(() => {
        consoleSpy = jest.spyOn(console, 'log').mockImplementation(() => {}); // Suppress actual log output
    });

    afterEach(() => {
        consoleSpy.mockRestore(); // Restore original console.log
    });

    test('should log a specific message', () => {
        const myFunc = () => {
            console.log('Hello from my function!');
        };
        myFunc();
        expect(consoleSpy).toHaveBeenCalledWith('Hello from my function!');
    });
});
*/